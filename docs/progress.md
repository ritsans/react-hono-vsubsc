# 進捗メモ

更新日：2026-09-26

## 現在地

- Better Auth による認証、サブスクの CRUD API と追加・一覧・削除の画面を実装済み。Cloudflare Workers に初回デプロイ済みで、本番の認証・追加・一覧・削除は動作確認済み。本番 URL：`https://react-hono-vsubsc.dazzling3season.workers.dev`。
- 円換算と EUR 対応は実装・コミット済み（`82bd405`）。「今月の支払い」は月払いすべてと、次回請求日の月が今月に一致する年払いを対象にする。ボタン操作時に必要な外貨のレートを Frankfurter から取得する。
- 安定性改善もコミット済み（`299f5fa`）。ユーザー切り替え・古い一覧応答への対処、通信失敗の表示と一覧再取得、多重操作防止、登録・削除時の円換算結果の無効化、API の金額・実在日付の検証を実装。設計意図は `docs/plans/2026-09-25-mvp-stability-design.md`。
- API の不正 JSON・UUID を 400、為替 API の通信失敗・不正応答を 502 として扱う修正を追加。テスト40件、`pnpm lint`・`pnpm check` 通過。これらの変更と円換算・EUR 対応、安定性改善のデプロイ状況は未確認。
- Neon にはマイグレーション `0005` まで適用済み。EUR 用の `0006` は生成済みだが、適用状況は未確認。
- 開発用と本番用で同じ Neon データベースを共有している。

## 次にやること

1. `0006` の適用状況とデプロイ状況を確認し、未反映の変更を反映する。
2. 編集（PUT）の画面、通貨別の集計表示、サービス名のサジェスト・公式リンク・ブランドアイコン、アラート通知など、`docs/overview.md` の残りの機能を進める。

## 未決事項

- 円換算の丸め方：`.claude/plans/exchange-rates.md` の要件の「切り捨て」と実装の「四捨五入」のどちらに揃えるか。
- 円換算の集計単位：現状は今月の対象に絞って月額と年額を合計する支払額ベース。月額換算などに揃えるかは未決定。
